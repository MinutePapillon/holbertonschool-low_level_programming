/**
 * _atoi - function that convert a string to an integer
 * @s: pointeur
 *
 * Return: always 0
 */
int _atoi(char *s)
{
	int i = 0;

	while (s[i] != '\0')
	{
		if ((s[i] >= '0') && (s[i] <= '9'))
				return (s[i]);
	}
	return ()
}
