#include <stdio.h>

/**
 * mul - function that multiplies two integers
 * @a: first operande
 * @b: second operande
 *
 * Return: returns the product of 2 integers
 */
int mul(int a, int b)
{
	return (a * b);
}
