#include "main.h"

/**
 * _isalpha - Function
 *
 * @c: nombre a tester
 * Return: Always 0.
 */
int _abs(int n)
{
	int resultat;

	if ((c >= 97 && c <= 122) || (c >= 65 && c <= 90))
	{
		resultat = 1;
	}
	else
	{
		resultat = 0;
	}
	return (resultat);
}
