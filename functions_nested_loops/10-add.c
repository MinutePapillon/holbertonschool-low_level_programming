#include "main.h"

/**
 * add - Function that adds two integers and returns the result
 *
 * @a: nombre a tester
 * @b: nombre a tester
 * Return: return the resultat
 */
int add(int a, int b)
{
	return (a + b);
}
